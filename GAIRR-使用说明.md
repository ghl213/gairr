<!-- GAIRR 开源版 · 功能/安装/使用说明（简洁版）
     定位：主体开源（GUI/CLI/Server 外壳、工具体系、技能/插件、市场生态）；
           核心引擎 GAIRR.Agent.dll（对话循环 / 四层检索 / 符号索引 / LSP / 编排执行）
           不提供源码，以编译后的二进制随发布包分发。
           密钥等私有内容不入库，统一用占位符。 -->

# GAIRR —— 本地 Agent 开发助手（开源版说明）

GAIRR 是运行在 Windows 上的桌面 Agent：以自然语言驱动代码检索、修改、编译验证的全流程，兼具 GUI 对话、命令行无头执行、技能/插件扩展与多 Agent 编排能力。

> 本说明面向 **GAIRR 开源版**：GUI/CLI/Server 外壳、工具体系、技能与插件、市场生态等主体代码开源；**核心引擎 GAIRR.Agent.dll（对话循环 / 四层检索 / 符号索引 / LSP / 编排执行）不提供源码**，以编译后的二进制随发布包分发，功能与接口不受影响。

## 一、功能一览

| 能力 | 说明 |
|---|---|
| 对话式开发 | 自然语言下达任务，Agent 自动检索→改码→编译验证→总结 |
| 语义代码定位 | 项目地图 / 符号检索 / 语义搜索（SmartSearch），中文查询也可 |
| 文件引用标记 | 输入 `@文件路径` 直接引用文件，`@dir` 引用目录 |
| 技能（Skill） | `skills/<名>/SKILL.md` 定义流程，命中描述自动触发，或 `/技能名` 强制触发 |
| 插件（Plugin） | `plugins/<名>/` 挂载自定义工具，可即装即用 |
| 市场 | 内置市场目录一键安装技能/插件；支持从 GitHub 安装社区 SKILL.md 规范技能 |
| 网页模型通道 | 内嵌网页窗口驱动真实模型站点（免 ApiKey，登录态自动持久化） |
| 多 Agent 编排 | 按功能架构树拆解任务，子 Agent 并行执行、审查汇总 |
| 自动任务 | 每日/工作日/每周/每月/Cron 周期触发后台任务，项目级隔离 |
| 消息推送 | 钉钉群消息插件（可选） |
| CLI 无头模式 | 脚本/计划任务友好，无需界面 |

## 二、安装

### 环境要求
- Windows 10/11（x64）
- 从源码构建：.NET 8 SDK
- 仅运行 CLI（框架依赖版）：.NET 8 运行时

### 从源码构建（发布单文件版）

```bat
:: 仓库根目录
build.bat
```

产物：
- `publish\GAIRR.exe` —— GUI 单文件版（自包含，无需预装运行时），构建完成后自动启动
- `publish\cli\gairr-cli.exe` —— CLI 无头宿主（框架依赖版，体积小）

构建脚本自带发布前快照（`back\pub_pre_时间戳\`）、产物自检与缺失补齐，发布失败可对照快照回滚。

### 直接运行
- GUI：双击 `publish\GAIRR.exe`
- CLI：

```bat
publish\cli\gairr-cli.exe -p <项目根目录> --ask "任务描述"
```

## 三、配置（config.ini）

程序读取 **exe 同目录** 的 `config.ini`（仅本地保存，**切勿提交真实密钥**）。最小可用配置：

```ini
[LLM]
Provider=DeepSeek            ; 当前使用的厂商

[DeepSeek]
BaseUrl=https://api.deepseek.com/v1
ApiKey=<你的密钥>
Model=deepseek-v4-flash
MaxTokens=128000
```

- 支持多家厂商（百炼 / Kimi / DeepSeek / OpenAI / Claude / Gemini / 智谱 / MiniMax / 本地 Ollama·LM Studio…），每厂一节，标题栏下拉切换。
- 不想配 ApiKey？用**网页通道**驱动真实站点：

```ini
[WebChannels]
List=deepseek-专家|deepseek|https://chat.deepseek.com/
DeepThink=1
Search=1
```

- 思考模式：`[LLM] ThinkingMode=1` 总开关，标题栏可切换。

## 四、使用

### GUI 基本用法
1. 打开 GAIRR，选择/确认工作项目根目录；
2. 输入任务（如「修复登录页样式」），Agent 自动规划、调工具、边做边验；
3. 有选项的决策会以卡片点选，长文本要求才需手打。

常用输入技巧：
- `@src\Foo.cs` —— 引用文件，Agent 先读它
- `@dir src\` —— 引用目录
- `/frontend-design` —— 强制触发指定技能

### 技能
- 触发：描述匹配自动加载，或 `/技能名` 强制；
- 自带示例：`frontend-design`（界面设计指引）、`long-file-edit`（大文件安全编辑）；
- 自定义：新建 `skills\<名>\SKILL.md` 即可，无需重启。

### 插件与市场
- 内置插件示例：钉钉消息、HackerNews/GitHub 热榜摘要；
- 安装：`MarketList` 查看目录 → `MarketInstall <名>` 安装；
- GitHub 社区技能：`GhSearch <关键词>` 搜索 → `GhInstallSkill <owner/repo>` 安装；
- 自研插件：按私有格式封装 `plugins\<名>\plugin.ini`，放入 `plugins\` 即注册。

### CLI 无头模式
```bat
:: 无 UI 执行单个任务（退出码：0 成功 / 1 失败 / 2 取消超时）
gairr-cli.exe -p D:\work\myproj --ask "把 README 翻译成英文"

:: GUI 程序也支持命令行参数
GAIRR.exe -t "任务描述"          :: 无 UI 跑任务
GAIRR.exe -plan <计划文件>       :: 无 UI 串行执行编排计划
```

### 自动任务
界面中新增定时任务：标题 + 提示词 + 周期（每日/工作日/每周/每月/Cron）。任务存于项目级 `.gairr\tasks.json`，随项目隔离，支持手动立即执行一次。

### 多 Agent 编排
维护 `.gairr\architecture.json` 功能架构树（L0 项目 → L1 子系统 → 功能叶子），按叶子拆解生成执行计划：子会话并行干活 → 审查 → 汇总，执行树状态实时可见。

> 典型任务的完整操作示例（Bug 修复 / 新功能 / 大文件重构 / 无头定时 / 新项目编排 / 复杂功能编排）见「典型开发场景示例」一章。

## 五、GUI 界面布局导览

主窗口自上而下、自左而右可分为四个区域：

**① 标题栏（窗口顶部）**
- **工作模式选择器**（左）：默认「🤖 自动匹配（角色-模式）」；`roles/<角色名>/role.json` 角色包免编译热加载，自定义"谁在用 + 怎么干活"。
- **模型切换下拉**（右）：按厂商列出全部已配置模型，当前在用模型金色高亮；未配 ApiKey 的选项灰显标注"未配置"；调用异常（额度/网络）的模型带 ⚠异常 红标，点击即重试。
- **GPU 监控三色柱**（最右，本地模型时启用）：显存 / 利用率 / 温度，绿→琥珀→红按阈值变色，悬停看第一卡明细。

**② 左侧栏（347px 可拖宽，五个页签）**

| 页签 | 内容 | 典型操作 |
|---|---|---|
| **项目** | 项目选择框 + "添加项目 +"；项目文件树带 git 状态着色 | 右键文件：加入到会话 / 加入自动维护文档 / 刷新提交记录 |
| **架构** | 功能架构树（`.gairr/architecture.json`：L0 项目 → 子系统 → 功能叶子） | 右键节点：更新架构树 / 在此功能新建编排任务 / 开始·暂停·停止 / 标记通过·跳过 / 查看叶子会话历史 |
| **会话** | 会话列表（带未读角标）、会话历史、任务列表、任务执行历史、会话文件变动 | "+ 新会话"开新窗格；随时切换回看历史 |
| **任务** | 自动任务（周期触发的后台任务） | "新增"弹窗录 标题 + 提示词 + 周期 + 工作模式；支持"立即执行" |
| **技能** | 五组：Agent 管理（内置工具）、内置技能、声明式插件、Skill 库（本地 skills/）、市场（已安装 / 在线热度排行 / 在线搜索） | 任一条目右键"加入到会话"；市场条目用 `MarketInstall` 一键安装 |

**③ 对话区（中央主栏）**
- 顶部小字同步显示当前 角色 + 模式 + 模型。
- 消息流展示对话与工具卡片（question-card / diff / 进度）；失败气泡带「重试」入口。
- 底部输入区：**发送** ↔ **停止** 同位互斥（运行中"停止"接管）；发送钮右侧下拉 = 「打开新会话 / 转为编排会话」；编排会话额外出现 **🎯 生成任务树**（收集期）/ **🎯 生成方案**（多模型决策）。
- 编排会话时右列出现编排面板，双 Tab：**编排方案**（L0 目标 + doc.md 实时预览）/ **编排执行**（执行树，右键 复核自动修复、开始/继续/停止、标记通过/跳过/撤销）。

**④ 代码查看器（对话区下方浮层，与编排面板互斥）**
- 左侧 git 变更文件树（双击文件贴树浮出提交详情），右侧逐文件 diff（+绿 / -红），右键可复制整段 diff。

> 左栏底部还有「手机端」开关：开启后本机 Server + 公网隧道启动，手机经 app.gairr.com 实时观看桌面会话执行流。

## 六、典型开发场景示例

六个由浅入深的场景，展示不同任务形态下人与 Agent 如何配合。通用心法：**先说清目标、边界、验收**，有选项时点卡片，长文本要求才手打。

### 场景 1：Bug 定位与修复（对话式，最常见）

**任务**：用户反馈"订单列表翻到第 2 页后，数据重复显示上一页"。

**步骤**：
1. **确认工作目录**：打开 GAIRR，确认当前项目根目录正确（状态栏可见）。
2. **描述现象**：输入——
   > 订单列表翻页时重复显示上一页数据，帮我定位根因并修复，改完要能编译通过。
3. **观察定位过程**：Agent 自动走语义检索（SmartSearch / MapTrace），以卡片展示命中的文件与方法，**先读代码再给结论**。
4. **补充上下文（可选）**：如有报错堆栈直接粘贴；或 `@src\OrderService.cs` 指定它重点看的文件，减少盲目检索。
5. **方案决策**：定位完成后会弹 question-card（如"只改分页参数（保守）/ 顺手重构查询构造（推荐）"）——点选即可。
6. **边做边验**：Agent 用 Edit 修改后自动跑编译；失败会自我修正重试，过程在卡片里可见。
7. **收尾确认**：阅读 diff 总结，认可即完成；不满意直接说"回滚"或"改用方案 B"。

**要点**：一次把"现象 + 期望结果 + 验收标准（能编译）"说清楚，比事后多轮追问省时间；`@文件` 是减少检索范围的最有效手段。

### 场景 2：新功能开发（先分析、再确认、后动手）

**任务**：给订单模块加"按客户名称模糊搜索"。

**步骤**：
1. **先问不改（调研阶段）**：
   > 先浏览项目，告诉我订单模块现有的查询入口和扩展点，**先别改代码**。
   Agent 输出模块结构、建议改动的文件清单。
2. **确认方案**：描述需求"加按客户名称模糊搜索，列表页和 API 都要"。收到方案卡片（接口入参 / SQL 改动 / 前端筛选框）后点"按推荐方案执行"。
3. **分步执行**：Agent 建任务清单并逐步做：后端查询 → 接口 → 前端筛选 → 联调；每步完成后自动编译验证。
4. **中途纠偏**：任何一步看着不对，直接发消息打断："列表页搜索框放工具栏右侧，别放表格上方"——它会调整后续步骤。
5. **验收**：让它"完整构建一次并输出改动文件清单"，你逐项过目。

**要点**：大功能务必"先分析后动手"，第一步的调研输出就是后续验收的参照；中途纠正比做完返工便宜得多。

### 场景 3：大文件重构拆分（技能加持）

**任务**：`ReportService.cs`（1500+ 行）职责混乱，拆成 3 个类。

**步骤**：
1. **引用目标 + 给出硬约束**：
   > `@src\Services\ReportService.cs` 这个文件太大，按职责拆成数据查询 / 报表计算 / 导出三个类，公开方法和调用方代码保持不变。
2. **技能自动加载**：描述命中 `long-file-edit` 技能，自动采用"先读后改、分段替换、每段可回滚"的安全流程，避免整文件重写丢失内容。
3. **分段确认**：每拆完一个类会给出 diff 卡片，确认或提意见后再继续下一个。
4. **编译与引用检查**：拆分完成后自动编译，并用引用检索确认没有遗漏的旧调用点。
5. **回归验证**：让它跑一次现有测试或冒烟命令，确认行为一致。

**要点**：大文件改动的两大风险是"改丢内容"和"漏改调用方"——前者靠技能的安全编辑流程，后者靠编译 + 引用检索兜底。

### 场景 4：无头场景（脚本化 / 定时任务）

**子场景 A：脚本里一次性调用**
```bat
gairr-cli.exe -p D:\work\myproj --ask "检查所有 controller 的异常处理，输出问题清单，先别改代码"
if %errorlevel% equ 0 (echo 完成) else (echo 失败，退出码 %errorlevel%)
```
退出码约定：0=成功，1=失败，2=取消/超时——可直接接 CI 或批处理流程。

**子场景 B：每日定时任务 + 钉钉推送**
1. 确认 `plugins\dingtalk\` 插件已配置（Webhook 写入插件 config.ini）。
2. GUI 中"新增自动任务"：标题"每日构建巡检"，提示词——
   > 拉取最新代码，执行构建，失败则定位首个报错；把结果摘要发到钉钉群。
3. 周期选 Cron：`0 9 * * *`（每天 9 点），保存。
4. 任务存于项目 `.gairr\tasks.json`；当天想提前跑，点"立即执行"即可，无需等到定时点。

**要点**：无头模式适合"结论型"任务（出清单、出报告）；涉及代码修改的任务建议留在 GUI 里做，方便中途卡片决策。

### 场景 5：新项目调研 → 编排 → 开发（从 0 到 1）

**任务**：接手一个全新的开源项目（如一个 Python 爬虫仓库），快速摸清结构并加一个新功能。

**步骤**：
1. **建项目**：左侧栏「项目」页签 → "添加项目 +" 选择仓库根目录；等消息卡片确认项目地图生成完成。
2. **调研（普通会话）**：
   > 先全面调研这个项目：模块结构、入口、数据流、扩展点，输出结构化调研报告，先别改代码。
   随后可用对话追问："爬虫调度在哪？""配置项有哪些？"——把这份报告当作后续开发的需求基线。
3. **建架构树**：把调研结果沉淀为可拆解的架构树：
   > 按调研结果更新 .gairr/architecture.json，L0=项目，L1=子系统，叶子拆到最小可验证功能。
   在「架构」页签核对树形结构；双击节点可追问细节，右键"更新架构树"持续维护。
4. **编排开发**：架构树上右键目标叶子 →「在此功能新建编排任务」，填写目标描述；对话区点 **🎯 生成任务树**，在编排方案 Tab 核对 L0 目标与 doc.md，然后在执行树右键「开始执行」。
5. **人审收口**：子会话并行开发 → 审查 → 汇总，执行树状态实时可见；汇总 diff 在代码查看器逐文件过目，认可后右键「标记通过」。
6. **沉淀续用**：让它把本次改动同步进架构树节点备注与自动维护文档，下次会话直接续上。

**要点**：调研报告 = 需求基线，架构树 = 任务拆解基线——两个基线先立住，编排只是顺水推舟的执行器；新项目的"建项目→调研→建树"三步别跳过。

### 场景 6：复杂功能编排（跨模块大需求）

**任务**：给既有系统加"会员积分体系"（涉及账户、订单、权益、前端展示 4 个子系统）。

**步骤**：
1. **定边界（普通会话）**：
   > 我要加会员积分体系，覆盖加分规则、积分流水、余额查询、前端展示。先分析涉及哪些模块、有哪些扩展点，只输出方案别改代码。
2. **更新架构树**：确认方案后让它更新 `.gairr/architecture.json`——已有树只加新分支，不推倒重来；「架构」页签核对叶子拆分（如"积分规则引擎 / 流水记账 / 余额服务 / 前端积分页"）。
3. **开编排会话**：发送钮右侧下拉 →「转为编排会话」（或「打开新会话」选编排型），输入：
   > 按架构树会员积分分支生成任务树，各叶子并行开发，最后统一审查汇总。
   点 **🎯 生成任务树**，在编排方案 Tab 核对 L0 目标与 doc.md；想多模型投票决策时点 **🎯 生成方案**（multi）。
4. **执行与干预**：执行树右键「开始执行」；叶子会话并行推进，想临时插单直接在对话区发消息（主持人会挂起计划征求确认）；某叶子方向不对 → 右键「标记跳过」或「停止」，修正后「继续」。
5. **集中审查**：全部叶子完成后进入审查汇总——编排会话汇总各子会话改动，在代码查看器逐文件过 diff；有意见直接在对话区说"第 N 项驳回，理由是……"。
6. **验收归档**：审查通过后「标记通过」收口；把积分体系的接口约定写进自动维护文档（项目树右键"加入自动维护文档"），供后续任务引用。

**要点**：复杂功能的关键是"先树后跑"——架构树的叶子粒度决定并行度与审查成本；把人审放在叶子收口处（而非每一步），既不阻塞进度也不失控。

## 七、常见问题

- **启动报缺 dll / 启动失败**：用 `build.bat` 重新发布，脚本会自检补齐外置原生 dll 与 runtimes。
- **模型无响应**：检查 `config.ini` 对应厂商节的 `BaseUrl/ApiKey/Model`；或改用网页通道。
- **CLI 提示缺运行时**：安装 .NET 8 运行时，或改用自包含的 `GAIRR.exe -t`。
- **为什么仓库里看不到某些核心逻辑**：核心引擎 GAIRR.Agent.dll（对话循环 / 四层检索 / 符号索引 / LSP / 编排执行）不提供源码，以编译后的二进制随发布包分发；功能与接口不受影响，可通过技能/插件/公开 API 扩展。

## 八、许可

开源部分（GUI/CLI/Server 外壳、工具体系、技能/插件、市场生态）按仓库根目录 `LICENSE`（Apache License 2.0）执行；核心引擎 GAIRR.Agent.dll 为闭源二进制，仅随发布包分发、不提供源码，其使用条款以发布包内 `GAIRR.Agent-EULA.txt` 为准。`config.ini` 中的密钥为用户本地私有数据，请勿随仓库分发。
